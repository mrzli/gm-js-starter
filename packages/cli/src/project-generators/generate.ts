import { GenerateProjectInput } from '../types/project-generators/inputs/generate-project-input';

export async function generate(input: GenerateProjectInput): Promise<void> {
  console.log('Generate');
}
